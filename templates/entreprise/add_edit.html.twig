{% extends 'base.html.twig' %}

{% block contenu %}
<div class="cmini">
    {% if editMode %}
        <h1>Modification</h1>
    {% else %}
        <h1>Ajout</h1>
    {% endif %}

    {{form(form)}}
</div>
{% endblock %}