{% extends 'base.html.twig' %}

{% block contenu %}

    <h1>{{ salarie.nom ~ ' ' ~ salarie.prenom }}</h1>
    <p>
        date d'embauche: {{ salarie.dateNaissance | date('d/m/Y') }}<br>
        adresse: {{ salarie.adresse }}<br>
        {{ salarie.cp ~ ' ' ~ salarie.ville }}<br>
        <br>
        Embauché le: {{ salarie.dateEmbauche | date('d/m/Y') }}<br>
        entreprise: {{ salarie.entreprise.raisonSociale }}
    </p>
    <a href="{{path('salarie_index')}}" title="retour" class="btn btn-primary">Retour</a>
{% endblock %}

{# {% block body %}
    {{ dump(salarie) }}
{% endblock %} #}